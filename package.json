{
  "name": "vakit-namaz",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.single.html dist/index.html && mkdir -p docs && cp index.single.html docs/index.html && touch docs/.nojekyll",
    "dev": "npx --yes serve . -l 5173",
    "preview": "npx --yes serve dist -l 4173"
  }
}
